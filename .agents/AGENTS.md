# Workspace Rules

- **No Inline CSS**: Never use inline `style="..."` attributes or embedded `<style>` tags in HTML files. Always use external CSS files in `assets/css/`.
- **No Python Execution**: Do not execute Python scripts or Python terminal commands. Use file viewing/searching tools directly instead.
